# URL de l'API Go côté serveur (SSR). En prod (Docker): http://api:8080
API_URL=http://localhost:8081

# URL de l'API Go côté navigateur (SSE). Doit être joignable depuis le client.
NEXT_PUBLIC_API_URL=http://localhost:8081
